- name: ansible command vs shell module
  hosts: local
  connection: local
  tasks:
  - name: command module test
    ansible.builtin.shell: cat /etc/passwd | grep root
    register: users_output

  - name: display value assigned in register vaar
    debug:
      msg: "{{ users_output }}"