- name: print ansible facts
  hosts: local
  connection: local
  tasks:
  - name: print ansible fact variable
    ansible.builtin.debug:
       msg: "{{ ansible_facts }}"

  - name: check redhat
    ansible.builtin.debug:
      msg: "yes, package manager is redhat"
    when: ansible_facts.os_family == "redhat"
