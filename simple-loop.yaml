- name: loop exmples
  hosts: local
  connection: local
  tasks:
  - name: print names using loop count
    ansible.builtin.debug:
      var: "{{ item }}"
    loop:
    - ramesh
    - suresh
    - mahesh