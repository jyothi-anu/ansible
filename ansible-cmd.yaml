- name: ansible command module
  hosts: local
  connection: local
  tasks:
  - name: check command 
    ansible.builtin.command: date
    register: store_date

  - name: print date
    ansible.builtin.debug:
      msg: "{{ store_date.stdout }}"
      